{
  "id": "trag-cli-command-behavior-secrets",
  "category": "cli-command-behavior",
  "description": "CLI validation and pilot commands must not echo secret values.",
  "input": "Run a pilot with database and Neo4j credentials in environment variables.",
  "expectedSignals": ["redacted output", "env status only", "no raw credential echo"],
  "gate": "Pass only if command output reports set/missing state without values."
}
