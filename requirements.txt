# WorldClass RAG - Requirements
# Core dependencies for enterprise-grade RAG system

# Core framework dependencies
pydantic>=2.5.0
pydantic-settings>=2.1.0
typing-extensions>=4.8.0

# Vector and embeddings (required)
sentence-transformers>=2.2.2
chromadb>=0.4.18
numpy>=1.24.0
scikit-learn>=1.3.0

# Text processing (required)
tiktoken>=0.5.2
nltk>=3.8.1
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Document processing - PDF (optional but recommended)
pypdf>=3.17.0
# PyMuPDF>=1.23.0  # More robust PDF processing, uncomment if needed

# Document processing - Office formats (optional)
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2
pandas>=2.0.0

# Document processing - Images and OCR (optional)
Pillow>=10.0.0
pytesseract>=0.3.10  # Requires tesseract system package

# Advanced image processing (optional)
# opencv-python>=4.8.0  # Uncomment for advanced image processing

# LLM integrations (optional - choose based on needs)
openai>=1.6.0
# anthropic>=0.8.0  # Uncomment for Claude integration

# Language processing (optional but recommended)
spacy>=3.7.0
# python -m spacy download en_core_web_sm  # Run after installation

# API and web framework (for REST API)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0

# Data validation and serialization
pyyaml>=6.0.1
python-multipart>=0.0.6

# Monitoring and observability (optional but recommended for production)
prometheus-client>=0.19.0
structlog>=23.2.0

# Security and PII detection (optional but recommended for enterprise)
presidio-analyzer>=2.2.33
presidio-anonymizer>=2.2.33
cryptography>=41.0.0

# Utilities
rich>=13.7.0
click>=8.1.0
tqdm>=4.66.0
chardet>=5.2.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
black>=23.11.0
isort>=5.12.0
mypy>=1.7.0

# Performance optimization (optional)
# faiss-cpu>=1.7.4  # For large-scale vector search
# redis>=5.0.0      # For caching
# psutil>=5.9.0     # For system monitoring