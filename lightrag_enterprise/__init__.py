"""Enterprise extension layer for LightRAG.

This package is intentionally adjacent to the upstream `lightrag` core.  It
adds governance, agent contracts, domain modules, model routing, audit, and
observability without changing the retrieval engine internals.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
